// const isDev = import.meta.env.NODE_ENV === "development";

const config = {
  // isDev: isDev,
  apiUrl: "https://dashboard-online.ru" + "/api/" // isDev ? "http://11/api/" : "https://e1f5-95-24-166-207.ngrok-free.app"
};

export default config;
